<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name="com.clauderemote.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Claude Remote"
        android:theme="@style/Theme.ClaudeRemote"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name="com.clauderemote.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden|navigation"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.clauderemote.ReplyReceiver"
            android:exported="false" />

        <receiver
            android:name="com.clauderemote.PlayReceiver"
            android:exported="false" />

        <service
            android:name="com.clauderemote.KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeping SSH session alive in background" />
        </service>

        <!-- Receives Wearable Data Layer messages from the watch companion
             app (installed applicationId com.clauderemote.android, same as
             this app — required for Data Layer delivery; its code lives
             under the .wear namespace only). exported=true + the
             intent-filter below are what Play Services binds to; the
             BIND_LISTENER permission attribute is legacy/unnecessary on
             current Play Services (do not re-add it). pathPrefix "/" so one
             service handles every path (/ping, /reply, /approve) without
             further manifest changes. -->
        <service
            android:name="com.clauderemote.PhoneWearService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
